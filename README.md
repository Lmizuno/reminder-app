# reminder-app
Reminder app front end in react

## Commands
`npm run start` 

`docker build . -t client`

`docker run --rm -it -p 3000:3000/tcp client:latest`

## Pre commit commands

`npm run build` 