//Template of how it might look like

const ReminderService = {
    getReminders: async function(user) {
        
    },

    addReminder: async function(reminder, user) {
        
    }
};

export default ReminderService;